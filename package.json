{
  "name": "usagebar-sync-server",
  "version": "0.1.0",
  "description": "Open-source Sync Server for UsageBar / CodexBar Machine Sync (protocol v1).",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js",
    "admin": "node dist/admin.js"
  },
  "packageManager": "pnpm@10.32.1",
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.9"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
